# Copy to .env and adjust as needed
OLLAMA_VERSION=latest
# 11435, not Ollama's default 11434: avoids colliding with a native Windows/Mac
# Ollama install (many people already have one) also using the default port.
OLLAMA_PORT=11435
